interface:
  display_name: "Delegate Wave"
  short_description: "Keep Codex in control; delegate bounded work"
  default_prompt: "Use $delegate-wave with Codex or Claude as controller, DeepSeek Flash as the bounded worker, and DeepSeek Pro only as a difficult-task fallback. Independently verify the result."
