# Examples

`review-example.md` is what `scripts/render.py` produces after a scan, with the
company and handles changed. Read it to see the shape before your first run:
the "needs a human" block first, then ready drafts newest-first, each with the
excerpt it was scored on, the one-line reasoning, and the commands to mark it.
